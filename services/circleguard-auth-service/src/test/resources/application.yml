jwt:
  secret: "my-super-secret-dev-key-with-more-than-sixty-four-characters-for-safety-1234567890"
  expiration: 3600000

spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password: password
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
